apiVersion: 1
name: render-spec
scope: local
description: A private disclosure showing the playground article contract.
requires: []
themes:
  only:
    - distill
    - anthropic
props:
  title:
    type: string
    default: Render specification
    maxLength: 120
